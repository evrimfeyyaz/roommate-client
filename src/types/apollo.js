/**
 * Screen options for the sub-tabs, such as "Room Service" and "Restaurants" in
 * the "Food" section.
 *
 * These are the screens in `...TabNavigator`s which use `TabView`.
 */
export type ApolloDataObject = {
  loading: boolean
}