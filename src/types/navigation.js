import type { IconData } from '../../assets/iconData'

/**
 * Screen options for the main tabs, such as "Home," "Room Service," etc.
 */
export type MainTabOptions = {
  title: string,
  iconData: IconData
}